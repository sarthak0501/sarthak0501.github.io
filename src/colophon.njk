---
layout: layouts/base.njk
permalink: /colophon/
title: How this site is built — Sarthak Bichhawa
desc: "Eleventy, one source-of-truth file, a build check that refuses unsourced claims, self-hosted type, and a PDF printed from the same record."
ogImage: /og/colophon.png
ogImageAlt: "How this site is built"
---
<section class="page wrap-narrow">
  <nav class="crumb" aria-label="Breadcrumb"><a href="/">‹ Home</a></nav>
  <header>
    <h1 class="page-title">How this site is built</h1>
    <p class="standfirst">A site that shows numbers should be held to the same standard as the systems it describes. This page is how it is.</p>
  </header>

  <div class="case-body">
    <section class="case-sec">
      <h2>One source of truth</h2>
      <div class="prose">
        <p>
          Every number, date, title, and link on this site renders from a single file —
          <code>src/_data/facts.yaml</code> — synced to the résumé dated <strong>{{ facts.meta.resume_rev }}</strong>.
          Pages are <a href="https://www.11ty.dev/">Eleventy 3</a> + Nunjucks templates that interpolate
          from it; if a claim isn't in the file, it doesn't ship.
        </p>
      </div>
    </section>

    <section class="case-sec">
      <h2>A build check that refuses the deploy</h2>
      <div class="prose">
        <p>
          <code>scripts/receipts-lint.mjs</code> runs in CI on every push. It scans the built HTML for four
          kinds of problems: required claims that went missing, superseded figures from older résumé
          revisions that crept back in, internal jargon an outside reader couldn't evaluate, and internal
          links that point at pages the build never emitted. A contradiction between this site and the
          résumé is a broken build, not an embarrassment somebody eventually notices. The sources and
          every correction made along the way are listed on <a href="/receipts/">sources and corrections</a>.
        </p>
      </div>
    </section>

    <section class="case-sec">
      <h2>No internal jargon</h2>
      <div class="prose">
        <p>
          Everything here is described by what it does, never by its internal codename. A reader outside
          Microsoft can't evaluate an acronym, and a write-up that needs a glossary isn't evidence — it's a
          membership test. The translation table lives with the résumé source, and the build check enforces
          it: an internal name in the built HTML fails the deploy.
        </p>
      </div>
    </section>

    <section class="case-sec">
      <h2>One résumé</h2>
      <div class="prose">
        <p>
          There is exactly one résumé. The PDF is the canonical authored document, hosted at the same
          revision this site renders from, and the <a href="/resume/">/resume/</a> page is the same record
          with a print stylesheet, so what you print and what you download can't disagree.
        </p>
      </div>
    </section>

    <section class="case-sec">
      <h2>The stack, deliberately small</h2>
      <div class="prose">
        <ul>
          <li><strong>Eleventy 3 + Nunjucks</strong>, built and deployed by GitHub Actions to GitHub Pages.</li>
          <li><strong>Self-hosted type</strong>, latin subsets only: Inter for the interface and headings, <a href="https://fonts.google.com/specimen/Fraunces">Fraunces</a> for editorial accents, IBM Plex Mono for code. No font CDN.</li>
          <li><strong>Plain CSS and one small script</strong> — the mobile menu, the copy button, and the run-trace viewer. Every page works with JavaScript disabled; nothing animates on load.</li>
          <li><strong>Analytics:</strong> <a href="https://www.goatcounter.com/">GoatCounter</a> — no cookies, no fingerprinting, counts only.</li>
          <li><strong>Per-page preview images</strong> rendered by a build script from the same templates, committed with the site.</li>
        </ul>
      </div>
    </section>

    <section class="case-sec">
      <h2>What gets removed before publishing</h2>
      <div class="prose">
        <p>
          Every write-up is published against a written checklist: customer and account identifiers removed,
          internal system names generalized, absolute volumes stated as orders of magnitude or multiples of
          baseline — while preserving the decision structure, the trade-offs, and the numbers already on the
          public record. Where a write-up has corrected itself, the correction is dated and left visible under
          "What I got wrong".
        </p>
      </div>
    </section>

    <section class="case-sec">
      <h2>History</h2>
      <div class="prose">
        <p>
          This is the fifth edition: selected work, personal ownership, and the decisions behind the results.
          The repository preserves earlier versions:
          <a href="{{ facts.identity.github }}/sarthak0501.github.io">github.com/sarthak0501/sarthak0501.github.io</a>.
          Built at revision <code>{{ build.rev }}</code>, {{ build.updated }}.
        </p>
      </div>
    </section>
  </div>
</section>
