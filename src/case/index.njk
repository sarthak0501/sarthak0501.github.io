---
layout: layouts/base.njk
permalink: /case/
title: Work — Sarthak Bichhawa
desc: "Six systems in production at Microsoft Azure Storage, each written up with the architecture, one artifact worth inspecting, results, and what went wrong."
ogImage: /og/case-index.png
ogImageAlt: "Work — six systems in production, written up with the architecture"
---
<section class="page wrap">
  <nav class="crumb" aria-label="Breadcrumb"><a href="/">‹ Home</a></nav>
  <header class="list-head">
    <h1 class="page-title">Work</h1>
    <p class="standfirst">Six systems in production at Microsoft Azure Storage — each with users, an on-call rotation, and the number it moved. Every one opens to the architecture, the evals, and what went wrong.</p>
  </header>
  <ul class="cards">
    {%- for c in facts.cases %}
    {%- set proof = "" %}
    {%- for s in facts.systems %}{% if s.link == c.slug and not proof %}{% set proof = s.proof %}{% endif %}{% endfor %}
    <li>
      <a class="card" href="{{ c.slug }}">
        <span class="card-cat">{{ c.tag }}</span>
        <span class="card-title">{{ c.title }}</span>
        <span class="card-text">{{ c.tease }}</span>
        <span class="card-proof">
          <span>{{ proof }}</span>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </span>
      </a>
    </li>
    {%- endfor %}
  </ul>
</section>
