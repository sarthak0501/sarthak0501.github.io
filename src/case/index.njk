---
layout: layouts/base.njk
permalink: /case/
title: Work — Sarthak Bichhawa
desc: "Six case studies from Microsoft Azure Storage: applied AI, revenue attribution, data reliability, embeddings, and experimentation. Architecture, ownership, results, and lessons."
ogImage: /og/case-index.png
ogImageAlt: "Work — case studies from Microsoft Azure Storage, with architecture and outcomes"
---
<section class="page wrap">
  <nav class="crumb" aria-label="Breadcrumb"><a href="/">‹ Home</a></nav>
  <header class="list-head">
    <h1 class="page-title">Work, in detail.</h1>
    <p class="standfirst">Six systems I’ve worked on at Microsoft Azure Storage. Each case study covers what I owned, the architecture, the decisions, and the results. Production status is listed for each project.</p>
  </header>
  <ul class="cards">
    {%- for c in facts.cases %}
    {%- set proof = "" %}
    {%- set projectStatus = "" %}
    {%- for p in collections.all %}{% if p.url == c.slug %}{% set projectStatus = p.data.status %}{% endif %}{% endfor %}
    {%- for s in facts.systems %}{% if s.link == c.slug and not proof %}{% set proof = s.proof %}{% endif %}{% endfor %}
    <li>
      <a class="card" href="{{ c.slug }}">
        <span class="card-cat">{{ c.tag }}</span>
        <span class="card-status">{{ projectStatus }}</span>
        <span class="card-title">{{ c.title }}</span>
        <span class="card-text">{{ c.tease }}</span>
        <span class="card-proof">
          <span>{{ proof }}</span>
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/></svg>
        </span>
      </a>
    </li>
    {%- endfor %}
  </ul>
</section>
